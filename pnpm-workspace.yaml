packages:
  - "apps/*"
  - "packages/*"
# esbuild ships prebuilt native binaries and needs its install script to run.
# It is a transitive dependency of vitest and tsx.
allowBuilds:
  esbuild: true
patchedDependencies:
  satellite.js@7.1.0: patches/satellite.js@7.1.0.patch
